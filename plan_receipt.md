# 올리브영 AI 뷰티 휴먼 - [위치 영수증 출력] 기능 구현 계획서

본 문서는 AI 가상 뷰티 부스 내에 탑재된 **스마트 영수증 티켓 출력 모듈**의 구현 계획서입니다. 실시간 추천 상품 및 진열 매대 맵 매칭 데이터 추출, 감각적인 3D 돌출 애니메이션 및 Web Audio API를 활용한 프린터 작동 사운드 구현을 포함합니다.

---

## 1. 주요 기능 정의
* **동적 영수증 정보 추출**: 사용자와 상담원의 대화 내역 중 가장 마지막에 활성화된 추천 제품 목록(`conversationState.recs`)을 실시간으로 가져와 영수증 품목 리스트에 바인딩합니다. 피부 진단기가 활성화된 경우 진단 점수 및 피부 타입 판정 데이터도 함께 영수증 헤더에 바인딩합니다.
* **열전사 영수증 스케치 및 모방**: 마크다운 톱니 찢김 테두리(Torn Edge) 디자인과 가상 고유번호 발급, 영수증 폰트 배치를 통해 매장 영수증의 날카롭고 빈티지한 감각을 시뮬레이션합니다.
* **3D 돌출 인쇄 애니메이션**: "영수증 티켓 출력하기" 클릭 시, 영수증 몸통이 프린터 틈새에서 아래로 웅장하게 밀려 나오는 3D Extrude 애니메이션(`receipt-extrude`)을 실행해 실제 영수증이 출력되는 듯한 깊은 현실감을 제공합니다.
* **Web Audio API 오디오 칩셋 합성**: 로컬 오디오 리소스 파일을 다운로드할 필요 없이 브라우저의 오디오 컨텍스트(`AudioContext`)를 사용해 열전사 도트 매트릭스 프린터 특유의 기어 걸림 사운드("치킹-치킹-지이이잉")를 주파수로 실시간 합성해 재생합니다.

---

## 2. 관련 파일 및 마크업 구조

### index.html
* **컨테이너**: `#modal-receipt` (상담 카드 영수증 모달 overlay)
* **영수증 틀**: `#thermal-receipt` (찢어진 상단/하단 테두리 포함)
* **메타 정보**: `#receipt-date-time` (실시간 날짜 정보 및 발급 일련번호), `#receipt-skin-score`, `#receipt-skin-type`
* **인쇄 아이템**: `#receipt-items-list` (카테고리명, 실제 상품명, 진열 매대 및 구역 정보 목록)
* **출력 실행**: `#btn-trigger-print` (영수증 출력 버튼) 및 `#print-success-feedback` (출력 성공 안내 피드백 창)

### style.css
* `.thermal-receipt-container`: 실제 인쇄지 질감을 내기 위해 흰색 그라데이션과 쉐도우 처리 및 찢김 패턴 이미지(linear-gradient 반복 마스킹 기법) 적용.
* `@keyframes receipt-extrude`: `transform: scaleY(0)`에서 시작해 `scaleY(1)`로 위에서 아래로 부드럽게 펼쳐지며 내려앉는 인쇄 돌출 키프레임 정의.

### app.js
* `setupPrintReceipt()`: 추천 제품이 없을 시 디렉토리의 기본 제품(닥터지, 피지오겔)을 삽입하고, 존재 시 `conversationState.recs`를 루프 돌려 위치 영수증을 동적으로 채움.
* `playPrintSoundEffect()`: `window.AudioContext` 생성. 트라이앵글 오실레이터 주파수(600Hz ~ 1100Hz)를 주기적으로 0.1초 간격으로 변조하고, 게인 노드를 활용해 0.09초간 쇠락하는 기계식 스텝 모터 마찰음을 즉석에서 오디오 하드웨어로 출력.

---

## 3. 검증 방법 (Verification Plan)
1. **영수증 내용 동적 갱신**: 대화창에서 "아누아 토너"를 추천받은 직후 영수증 모달을 열었을 때, 영수증 리스트 항목에 "1. 아누아 어성초 77 토너 - A구역 A-2 진열대"가 정확히 꽂혀 있는지 확인합니다.
2. **사운드 합성 재생**: 인쇄 실행 버튼 클릭 시 귀로 들리는 리얼한 매트릭스 도트 프린터 인쇄 사운드("드르륵-치킹")가 끊김 없이 스피커로 흘러나오는지 검증합니다.
3. **돌출 애니메이션 시각 효과**: 인쇄 시작 시 영수증 상단이 토출구 밑으로 부드럽게 미끄러지듯 돌출되어 내려오는 애니메이션 흐름을 육안으로 확인합니다.
