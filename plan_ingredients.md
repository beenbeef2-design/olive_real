# 올리브영 AI 뷰티 휴먼 - [성분 분석] 기능 구현 계획서

본 문서는 AI 가상 뷰티 부스 내에 탑재된 **EWG 스마트 성분 분석기**의 구현 계획서입니다. 화장품 전성분 데이터 조회, EWG 안전도 등급 분류, 피부 타입별 성분 적합도 분석 기능 개발에 관한 상세 명세입니다.

---

## 1. 주요 기능 정의
* **제품명 성분 동적 검색**: 데이터베이스에 등록된 실제 제품명(예: 피지오겔 크림, 닥터지 수딩크림 등) 혹은 추천 태그를 입력해 전성분을 빠르게 로드합니다.
* **가상 제품 실시간 분석 엔진 연동**: 데이터베이스에 없는 새로운 올리브영 화장품명을 입력하더라도 가상 제품 생성 엔진(`generateVirtualProduct`)과 연계해 신뢰성 있는 가상의 EWG 안전도 전성분 등급표를 즉각적으로 합성해 냅니다.
* **EWG 위험도 카운트 시각화**: EWG 그린(안전, 1-2등급), 옐로우(보통, 3-5등급), 레드(주의 필요, 6등급 이상) 성분 개수를 카운팅하여 상단 3색 대시보드 카드에 실시간 출력합니다.
* **다차원 피부 적합성 판정**: 건성 피부, 지성 피부, 민감성 피부에 성분 배합 목적이 맞춤형으로 적합한지 판정 등급(매우 추천, 보통, 주의 필요)과 한글 사유 멘트를 띄워줍니다.
* **전성분 세부 등급 테이블**: 전성분의 영문명, 한글명, EWG 안전도 원형 등급 배지, 배합 목적(보습, 향료, 방부제 등) 및 부가 설명을 일목요연한 표(Table)로 렌더링합니다.

---

## 2. 관련 파일 및 마크업 구조

### index.html
* **컨테이너**: `#modal-ingredients` (스마트 성분 분석 연구소 모달 overlay)
* **검색바**: `#ingredient-search-input` 및 `#btn-ingredient-search` (분석 실행 단추)
* **퀵 링크**: `.btn-suggest-search` (추천 제품 빠른 분석 트리거 단추들)
* **결과 패널**: `#ingredient-result-box` (제품 메타 및 EWG 3색 요약 카드, `.compatibility-alerts-box` 피부 타입별 적합성 영역, `#ingredients-table-body` 세부 테이블)

### style.css
* `.safety-summary-cards`: 그린/옐로우/레드 등급을 각각 직관적인 컬러 보더와 투명 배경으로 정렬한 대시보드 스타일.
* `.ewg-badge-label`: EWG 등급 수치에 맞춰 녹색, 황색, 적색 네온 광채와 둥근 배지를 부여하는 스타일.

### app.js
* `loadIngredientReport(prodId)`: 지정 제품의 전성분 배열(`ingredientsList`)과 EWG 요약 데이터를 바인딩하여 테이블 바디에 HTML 코드를 동적으로 조립/생산.
* `#btn-ingredient-search` 이벤트 리스너: 입력값을 정제하여 일치하는 내부 제품을 찾거나, 가상 제품을 빌드하여 즉각 분석 결과를 표출하도록 흐름을 연동.

---

## 3. 검증 방법 (Verification Plan)
1. **제품 검색 및 결과 갱신**: 검색창에 "닥터지"를 입력한 후 "성분 분석하기"를 눌렀을 때, 닥터지 제품 정보와 EWG 요약 등급(그린 18, 옐로우 2)이 누락 없이 대시보드에 업데이트되는지 검증합니다.
2. **동적 가상 제품 성분 생성**: DB에 없는 화장품(예: "아이오페 스킨") 검색 시 가상 제품 엔진이 작동하여 "아이오페 스킨"에 대한 EWG 전성분 표가 정상 합성되어 오류 없이 화면에 로드되는지 확인합니다.
3. **EWG 배지 컬러 일치**: 등급 수치(1, 4, 6 등)에 따라 EWG 배지 배경색이 그린, 옐로우, 레드로 정확히 치환되어 렌더링되는지 디자인 요소를 체크합니다.
