# 올리브영 AI 뷰티 휴먼 - 아바타 영상 대체 및 맞춤 추천 고도화 구현 계획서 (최종본)

본 계획서는 키오스크 AI 휴먼의 정적 이미지 아바타를 사용자가 제공한 실시간 움직이는 동영상 파일들(여성용 `avatar_female.mp4`, 남성용 `avatar_male.mp4`)로 대체하되, 컨테이너 영역에 여백 없이 꽉 들어차도록 화면 크기에 맞추며, 남성과 여성의 촬영 구도 차이를 감안해 **남성 컨설턴트는 양쪽 어깨 전체가 단 1픽셀도 잘리지 않도록 비율을 100% 유지(`object-fit: contain` 및 `scale(1.0)`)하도록 보정**하며, 첫 화면의 임시 계획서 버튼을 제거하고 피부 타입별 맞춤 추천을 통합 설계하는 상세 구현 계획입니다.

---

## 핵심 개선 방향 및 기술 설계

### 1. 남/여 개별 아바타 동영상(Video) 연동 및 어깨 노출 보정 (`index.html`, `style.css`, `app.js`)
* **마크업 변경 (`index.html`)**:
  - 여성 전용 비디오(`assets/avatar_female.mp4`)와 남성 전용 비디오(`assets/avatar_male.mp4`)를 개별 주입합니다.
* **성별 맞춤형 화면 채움 및 검은 테두리 차단 (`style.css`)**:
  - 비디오 재생 영역이 아바타 부모 박스에 가로세로 여백 없이 딱 맞물리도록 `width: 100%`, `height: 100%`, `top: 0`, `left: 0`을 기본으로 지정합니다.
  - **여성(이지아) 비디오**: `scale(1.08)` 크롭과 `object-position: center 20%` 정렬 및 `avatar-breath` 호흡 연동.
  - **남성(민준우) 비디오**: 남성 캐릭터의 어깨 골격과 상반신이 전혀 가려지지 않도록 **`object-fit: contain;`** 으로 비율을 100% 보존합니다. 스케일을 크롭 없이 원본 배율인 **1.0배율**(`scale(1.0)`)로 초기화하고, 중심점도 정중앙(`center center`)으로 보정합니다. 호흡 애니메이션도 원본 비율 기준인 `avatar-breath-male`(`scale(1.0)` ~ `scale(1.015)`)로 맞추어 어깨 잘림을 원천 방지합니다.
  - 비활성화된 비디오는 `opacity: 0`으로 숨겨두고, 활성화된 아바타 비디오(`.avatar-video.active`)만 `opacity: 1`과 `pointer-events: auto`를 적용하여 젠더 전환 시 페이드인-아웃 효과를 줍니다.
* **동작 제어 (`app.js`)**:
  - 젠더 변경 탭을 누르면 선택된 젠더에 맞게 `#avatar-female-video`와 `#avatar-male-video`의 `active` 클래스를 교체하는 제어 로직을 구현합니다.
  - 음성 발화(TTS) 시에는 현재 노출되고 있는 활성 비디오(`.avatar-video.active`)의 재생 속도(`playbackRate = 1.25`)를 가속해 모션을 연동하고, 종료되면 일괄 속도를 정상화(`1.0`)합니다.

### 2. 첫 화면(Welcome Splash View)에서 불필요한 계획서 버튼 제거 (`index.html`)
* 초기 화면의 디자인 무결성을 지키기 위해, 임시로 첫 화면 하단에 추가되었던 "피부 타입 추천 구현 계획서" 바로가기 버튼 링크를 마크업에서 전면 삭제하여 본래의 시작 버튼만 남도록 원복합니다.

### 3. 피부 타입 대화 라우팅 및 가상 제품 생성 가드 고도화 (`app.js`)
* 쿼리에 브랜드명이 포함되지 않고 단순히 피부 타입 키워드(`건성`, `지성`, `복합성`, `수부지`)만 감지되는 경우, 가상 제품 생성을 스킵하고 **피부 타입별 전용 추천 분기(2.5 ~ 2.8)**를 최우선으로 타도록 변경합니다.

---

## 변경이 적용될 파일

* **index.html**: 여성용/남성용 개별 비디오 태그 주입 및 Welcome View의 계획서 버튼 삭제.
* **style.css**: `.avatar-video.active` 100% 꽉 채우는 cover 구도 비율, 남성 아바타 전용 어깨 보존용 object-fit: contain 및 scale(1.0) 설정 추가.
* **app.js**: 젠더 스위칭 시 개별 비디오 active 제어, 대화 라우터 분기 및 가상 제품 필터링 수정.

---

## 검증 계획 (Verification Plan)

1. **남/여 개별 비디오 송출 검증**: 이지아(여성) 컨설턴트일 때 여성 영상이, 민준우(남성) 컨설턴트일 때 남성 영상이 각각 어색함 없이 교체 재생되는지 확인.
2. **남성 어깨 실루엣 노출 검증**: 민준우 활성화 시, `object-fit: contain`에 의해 아바타 비디오 내부의 넓은 양어깨와 상반신 전체 구도가 잘리는 부분 없이 100% 온전히 드러나는지 확인.
3. **첫 화면 버튼 삭제 확인**: `http://localhost:8090/` 접속 시 하단에 구현계획서 버튼이 제거되고 원래의 깨끗한 화면으로 시작되는지 확인.
4. **피부 타입별 맞춤 추천 정상화**: "건성", "지성", "복합성", "수부지" 입력 시 피지오겔, 닥터지 등의 정해진 추천 제품 매대 정보 및 실시간 지도가 정상 동작하는지 최종 확인.
